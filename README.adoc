== To trigger slowness

Change quarkus-roq-plugin-asciidoc-jruby to quarkus-roq-plugin-asciidoc

== Qute processing errors

Remove `quarkus.asciidoc.qute=true` from `application.properties` and try adding `qute: true` on posts.